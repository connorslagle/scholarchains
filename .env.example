# ScholarChains Frontend Configuration

# Backend API URL (required for OpenTimestamps operations)
VITE_API_URL=http://localhost:3001

# Nostr Relay Configuration (optional - defaults provided in code)
# VITE_DEFAULT_RELAY=wss://relay.damus.io

# Blossom Server Configuration (optional - defaults provided in code)
# Multiple servers can be specified, comma-separated for redundancy
# If one server fails, the app will automatically try the next one
VITE_BLOSSOM_SERVERS=https://blossom.primal.net,https://blossom.satellite.earth

# User can override and select preferred server in UI settings
# Leave empty to use all configured servers with automatic fallback

# Development Configuration
# VITE_HMR_PROTOCOL=ws
# VITE_HMR_HOST=localhost
# VITE_HMR_PORT=8080
